module.exports = {
	
	name: 'root',

	inherits: null,

	extend: function(sub) {
		var son = {
			super: this,
			inherits: this.name
		};

		for(var fn in sub) {
			son[fn] = sub[fn];
		}

		return son;
	}
	
}